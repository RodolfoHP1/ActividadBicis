extends ../layout 

block content
    a(href='/usuarios') Volver
    br
    h1 Login del usuario
    form(action='/usuarios/login' method='POST')
        table.table 
            tr 
                td
                    label(for='email') Email:
                td
                    input#email(type='text' name='email' value= usuario.email class=errors.email ? 'is-invalid' : '')
                    if errors.email
                        small#emailHelp.text-danger = errors.email.message
            tr 
                td
                    label(for='pwd') Password:
                td
                    input#pwd(type='password' name='password' value= usuario.email class=errors.password ? 'is-invalid' : '')
                    if errors.password
                        small#emailHelp.text-danger = errors.password.message
                td 
                td
                    input.btn(type='submit' value='OK')
